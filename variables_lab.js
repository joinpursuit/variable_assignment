// Problem One 
let birthYear = 1991
let futureYear = 2020
let ageMin = futureYear - birthYear
let ageMax =  ageMin - 1;
console.log('I will either be ' + ageMin + ' or ' + ageMax + ' in ' + futureYear);


// Problem Two
let currentAge = 28
let maxAge = 50
let snackAmount = 3
let snackYear = 365 * snackAmount
let overallSnack = snackYear * (maxAge - currentAge)
console.log("You will need " + overallSnack + " to last you until the age of " + maxAge)

// Problem Three
let radius = 24
let diameter = radius * 2
let circumferenceResult = Math.PI * diameter 
let areaofCircle = Math.PI * radius * radius
console.log('The circumference is: ' + circumferenceResult);
console.log('The area is: ' + areaofCircle);

//Problem Four

let celcius = 0;
let celciusInFahrenheit= (celcius * 1.8) + 32;
let fahrenheit = 32;
let fahrenheitInCelcius = (fahrenheit - 32) / 1.8;

//Problem Five

let aliceGrade = 95
let bodGrade = 90
let camGrade = 89
let gradeAverage = (aliceGrade + bodGrade + camGrade) / (3)
let deeGrade = 88
let newAverage = (aliceGrade + bodGrade + camGrade + deeGrade) /(4)
if (deeGrade > newAverage) {
  console.log(deeGrade) 
}

//Problem Five
1. Output will be 10
2. Output will be jello
3. Output will be String

